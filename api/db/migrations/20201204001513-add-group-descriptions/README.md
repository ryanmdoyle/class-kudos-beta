# Migration `20201204001513-add-group-descriptions`

This migration has been generated by Ryan Doyle at 12/3/2020, 4:15:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_PrimaryGroup" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "ownerId" TEXT NOT NULL,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_PrimaryGroup" ("id", "name", "ownerId") SELECT "id", "name", "ownerId" FROM "PrimaryGroup";
DROP TABLE "PrimaryGroup";
ALTER TABLE "new_PrimaryGroup" RENAME TO "PrimaryGroup";
CREATE TABLE "new_SecondaryGroup" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL DEFAULT '',
    "ownerId" TEXT NOT NULL,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_SecondaryGroup" ("id", "name", "ownerId") SELECT "id", "name", "ownerId" FROM "SecondaryGroup";
DROP TABLE "SecondaryGroup";
ALTER TABLE "new_SecondaryGroup" RENAME TO "SecondaryGroup";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201129221706-add-image-to-user..20201204001513-add-group-descriptions
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -75,16 +75,18 @@
 model PrimaryGroup {
   id                String              @id @default(uuid())
   name              String
+  description       String              @default("")
   owner             User                @relation(fields: [ownerId], references: [id])
   ownerId           String
   PrimaryEnrollment PrimaryEnrollment[]
 }
 model SecondaryGroup {
   id                  String                @id @default(uuid())
   name                String
+  description         String                @default("")
   owner               User                  @relation(fields: [ownerId], references: [id])
   ownerId             String
   SecondaryEnrollment SecondaryEnrollment[]
 }
```



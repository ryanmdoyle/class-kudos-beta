import { Link, routes } from '@redwoodjs/router'

const ClassGroupRedeemedPage = () => {
  return (
    <>
      <h1>ClassGroupRedeemedPage</h1>
      <p>
        Find me in{' '}
        <code>
          ./web/src/pages/ClassGroupRedeemedPage/ClassGroupRedeemedPage.js
        </code>
      </p>
      <p>
        My default route is named <code>classGroupRedeemed</code>, link to me
        with `<Link to={routes.classGroupRedeemed()}>ClassGroupRedeemed</Link>`
      </p>
    </>
  )
}

export default ClassGroupRedeemedPage
